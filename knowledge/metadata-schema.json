{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Knowledge Page Metadata Sidecar",
  "description": "Each markdown knowledge page has one JSON sidecar with this schema. Sidecars are regenerated from md frontmatter by scripts/rebuild_sidecars.py (md frontmatter is the single source of truth).",
  "version": "0.2.0",
  "type": "object",
  "properties": {
    "knowledge_id": {
      "type": "string",
      "description": "Unique ID, derived from filename slug (lowercase, spaces -> underscores)"
    },
    "title": {
      "type": "string"
    },
    "path": {
      "type": "string",
      "description": "Path to the source markdown file, relative to repo root (e.g. 知识库/xxx.md or knowledge/pages/xxx.md)"
    },
    "created": {
      "type": ["string", "null"],
      "format": "date"
    },
    "updated": {
      "type": ["string", "null"],
      "format": "date"
    },
    "type": {
      "type": ["string", "null"],
      "enum": [
        "analysis",
        "concept",
        "moc",
        "note",
        "tracker",
        null
      ]
    },
    "domain": {
      "type": ["string", "null"],
      "enum": [
        "agent-memory-cognition",
        "agent-systems-engineering",
        "ai-engineering",
        "ai-talent",
        "knowledge-management",
        "meta",
        null
      ]
    },
    "status": {
      "type": ["string", "null"],
      "enum": [
        "developing",
        "draft",
        "evergreen",
        "growing",
        "live",
        "mature",
        "seed",
        "stub",
        null
      ]
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "description": {
      "type": "string",
      "description": "One-line summary copied from md frontmatter (omitted when absent)"
    },
    "source_refs": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Merged from md frontmatter source/source_url: external references this knowledge was derived from"
    },
    "related": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Related pages declared in md frontmatter (may be wikilinks or plain page names), kept verbatim"
    },
    "dependencies": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "knowledge_ids this page depends on (for Dependency Graph); maintained manually, not derivable from md"
    },
    "outgoing_links": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Page names of wikilinks found in the md body ([[page]], [[page|alias]], [[page#anchor]]), deduplicated (for Knowledge Graph)"
    },
    "freshness_score": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Computed from updated: <=90d = 1.0, 90-180d = 0.7, 180-365d = 0.4, >365d = 0.1; unparseable/missing date = 0.5"
    },
    "source_tier": {
      "type": ["string", "null"],
      "enum": ["T1", "T2", "T3", "T4", null],
      "description": "入库标准 v0.1 来源分级：T1 一手学术/官方，T2 权威机构/一手工程，T3 自媒体/二手，T4 不明或本地；入库时由 executor 按来源域名自动打标"
    },
    "verification": {
      "type": ["string", "null"],
      "enum": ["unverified", "verified", "contested", "falsified", null],
      "description": "核实状态：unverified(默认) / verified / contested / falsified；升级需交叉印证或 T1 未撤稿，见 docs/ingest-standard-v0.1.md"
    },
    "verified_at": {
      "type": ["string", "null"],
      "format": "date",
      "description": "最近一次核实日期；unverified 时为 null；超过 180 天应降级复查"
    },
    "confidence": {
      "type": ["string", "null"],
      "enum": ["high", "medium", "low", null],
      "description": "置信度评估（AI 或人填，默认 low）"
    }
  },
  "required": [
    "knowledge_id",
    "title",
    "path",
    "created",
    "updated",
    "type",
    "domain",
    "status",
    "tags",
    "source_refs",
    "related",
    "dependencies",
    "outgoing_links",
    "freshness_score"
  ]
}
