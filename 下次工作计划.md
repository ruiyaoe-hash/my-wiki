# 下次工作计划 — 2026-07-16 收尾

## 当前进度

- [x] Phase 0：Ontology v0.1（3 文件，7 对象）
- [x] Phase 1：State Runtime v0.1（4 JSON + State Manager）
- [ ] Review：回头看 v0.1，修正 ontology.md
- [ ] Phase 2：Knowledge + Capability

## 下次进站操作（Agent 用）

1. 读 `state/session.json` → 状态为 ended，上次会话正常关闭
2. 读 `state/current-task.json` → 空闲
3. 读 `hot.md` → 了解上次做了什么
4. 确认 Review 还是直接进 Phase 2

## Review 清单（约 30 分钟）

三个问题：
- 7 个对象的定义有没有遗漏或模糊？（特别是 Memory 和 State 的边界）
- State Manager 的接口够不够？要不要加 merge_conflict_handler？
- hot.md 拆完之后，有没有信息丢失？

## Phase 2 做什么（如 Review 通过）

1. 建 `knowledge/` 目录 + metadata sidecar（每篇知识页一个 JSON）
2. 建 `source/` 目录结构
3. 四种 Graph 的 schema 定义
4. Protocol 格式升级（Markdown → YAML）
5. Protocol Executor（自动执行协议步骤）

## 关键决策点（下次要拍板的）

- 旧 `知识库/` 现在改名还是 Phase 2 结束后再改？
- Graph 四种先做哪个？（建议先 Dependency Graph，因为它最简单且立刻有用）
- Protocol Executor 用 Python 还是保持 YAML + Agent 解释？

## 进入命令

```bash
cd D:\my-wiki
git checkout develop
git pull origin develop
```
